import { PagePlaceholder } from '@/components/layout/PagePlaceholder'

export function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="LLM provider configuration, API key masking, connection tests, and review preferences will be implemented across M4 and M6."
    />
  )
}
